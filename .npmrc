# Link local workspace packages (e.g. @repo/*) instead of fetching from the
# registry. pnpm 9 defaults this to false, which is why a fresh `pnpm install`
# otherwise tries (and fails) to download @repo/typescript-config etc.
link-workspace-packages=true

# Node version is enforced only as a warning, not a hard failure.
engine-strict=false
